items = ("apple", "banana", "cherry", 67, True, 69.69)

for i in items:
    print(i)

# items[0] = "grape" # This will raise an error because tuples are immutable, so we cannot change the
# value of an item in the tuple by assigning a new value to it using its index.