import numpy as np

salary_list = [50000, 60000, 55000, 70000, 65000, 80000, 75000, 90000, 85000, 95000]

print(np.mean(salary_list)) # This will calculate the mean (average) of the salary_list using the mean() function from the NumPy library.